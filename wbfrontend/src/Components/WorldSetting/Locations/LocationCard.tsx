import { LocationInterface } from "../../../Interfaces/LocationInterface";

export const LocationCard: React.FC<LocationInterface> = (location: LocationInterface) => {
    return (
        <div>
        <h1>LocationCard</h1>
            <p>Welcome to the LocationCard page</p>
        </div>
    );
}